# 프로젝트 작업 지침

이 저장소에서 작업하는 모든 사람과 코딩 에이전트는 아래 규칙을 따른다.

## 구조 문서 유지

- 구현 작업을 시작하기 전에 `docs/GAME_ARCHITECTURE.md`를 읽는다.
- 코드를 수정할 때 기존 모듈의 위치, 책임, 의존 방향 또는 데이터 흐름을
  확인할 필요가 있으면 `docs/GAME_ARCHITECTURE.md`를 우선 참고한다.
- 필요한 구조 정보가 문서에 없다면 코드에서 근거를 확인한 뒤 구조 문서를
  보완하고, 그 기준에 맞춰 코드를 수정한다.
- 코드 구조, 책임 경계, 런타임 흐름, Scene, 상태 모델, 입력, 에셋 정책,
  저장 방식, 외부 서비스 또는 배포 방식이 바뀌면 같은 커밋에서
  `docs/GAME_ARCHITECTURE.md`도 갱신한다.
- 새 주요 디렉터리나 시스템을 추가하면 디렉터리 구조와 책임 설명에 반영한다.
- 기능을 완성하거나 제거하면 `현재 구현 상태` 체크리스트를 갱신한다.
- 중요한 설계 결정을 내리면 날짜와 이유를 `변경 기록`에 짧게 남긴다.
- 문서와 구현이 다르면 구현을 정답으로 방치하지 말고 둘을 같은 변경에서
  일치시킨다.

## 아키텍처 규칙

- 게임 규칙과 저장 가능한 상태는 `src/game`에 두고 Phaser와 분리한다.
- Phaser Scene은 얇게 유지하며 입력 연결, 렌더링, 카메라, 애니메이션 및
  이펙트를 담당한다.
- Scene의 Sprite, Tween 또는 Camera를 게임 상태의 원본으로 사용하지 않는다.
- 물리 키 입력은 `src/game/input`에서 명시적인 게임 액션으로 변환한다.
- 에셋 경로를 여러 파일에 직접 쓰지 말고 매니페스트 키로 관리한다.
- 복잡한 HUD, 메뉴와 설정 화면은 기본적으로 DOM 오버레이를 사용한다.

## 변경 및 검증

- 관련 없는 사용자 변경을 덮어쓰지 않는다.
- 의존성을 변경하면 `package.json`과 잠금 파일을 함께 커밋한다.
- 완료 전에 `npm run build`를 실행해 TypeScript 검사와 프로덕션 빌드를
  확인한다.
- 게임 플레이가 바뀌면 가능하면 브라우저에서 핵심 루프를 직접 확인한다.

## Git 기록

- 전체 소스와 필요한 에셋을 이 저장소에 유지한다.
- 생성된 `dist`와 `node_modules`는 커밋하지 않는다.
- 작업 내용을 설명하는 작고 의미 있는 커밋을 남긴다.
- 명시적인 요청 없이 기존 커밋 기록을 재작성하거나 합치지 않는다.

## 푸시 요청 처리

- 사용자가 명시적으로 푸시를 요청하면 현재 작업의 관련 변경을 검증하고
  커밋한 뒤 원격 저장소에 푸시하는 전체 절차를 수행한다. 단순 푸시 요청은
  관련 변경의 스테이징, 필요한 새 커밋 생성 및 현재 브랜치 푸시를 포함한다.
- 푸시 전 `git status -sb`, diff와 `git remote -v`를 확인해 변경 범위,
  현재 브랜치 및 대상 원격 저장소가 올바른지 검증한다.
- 작업 트리에 현재 요청과 무관한 사용자 변경이 섞여 있으면 임의로 함께
  스테이징하지 말고 범위를 먼저 확인한다.
- 커밋 전 `npm run build`를 실행한다. 실패하면 원인을 해결하고 다시
  검증하기 전에는 커밋하거나 푸시하지 않는다.
- 관련 파일만 스테이징하고 변경 전체를 설명하는 간결한 커밋 메시지를
  사용한다. 새 커밋이 필요하지 않으면 기존 커밋을 그대로 푸시한다.
- GitHub 작업 전 `gh auth status`로 인증을 확인한다. Windows에서 `gh`가
  `PATH`에 잡히지 않으면 설치되지 않았다고 단정하지 말고
  `C:\Program Files\GitHub CLI\gh.exe` 등 표준 설치 경로를 확인한다.
- 기본 원격 저장소는 `origin`이며 이 저장소의 주소는
  `https://github.com/poiu10/AIgame.git`이다. 사용자가 다른 대상이나 브랜치를
  지정하지 않았다면 현재 브랜치를 `origin`에 푸시한다.
- `--force` 또는 `--force-with-lease` 푸시, 커밋 수정, rebase 등 기록을
  바꾸는 작업은 사용자가 명시적으로 요청하지 않는 한 수행하지 않는다.
- 푸시 후 GitHub Actions 실행을 확인한다. Pages 관련 변경이면 배포 완료까지
  확인하고 `https://poiu10.github.io/AIgame/`이 정상 응답하는지도 검증한다.
- 워크플로가 실패하면 로그에서 원인을 확인하고 요청 범위 안의 안전한 수정은
  적용해 재검증한다. 권한, 인증 또는 저장소 설정 변경이 필요하면 그 사실을
  명확히 알리고 필요한 승인을 받는다.
- 완료 보고에는 브랜치, 커밋 해시, 푸시 대상, 실행한 검증 및 배포 URL을
  포함한다. 사용자가 요청하지 않았다면 새 PR은 만들지 않는다.
